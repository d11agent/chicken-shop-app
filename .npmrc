# RN/Expo transitive peer ranges lag a patch behind react-native (e.g. jest-preset
# 0.85 vs rn 0.86). These are peerOptional mismatches; legacy resolution is the
# standard, safe choice for Expo projects so `npm install` stays reproducible.
legacy-peer-deps=true
